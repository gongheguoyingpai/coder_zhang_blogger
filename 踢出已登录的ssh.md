#####踢出已登录的ssh

我们经常会利用ssh远程登录到服务器来进行程序开发或者其他方面的工作。
有时可能会因为很多原因，导致我们的ssh始终链接而没有断开。这时就需要
一些方法来解决掉如何快速断开大量存在的连接。

如下，是一种可行的方法，并且该方面是基于用户名进行过滤的，当然，也
可以使用其他的方式来进行数据的过滤。

脚本如下:
```
w | grep user_name | awk '{if ($5+0<=51 && $5+0>=1) system("sudo pkill -kill -t " $2)}'
```
